/** @type {import('next').NextConfig} */
const nextConfig = {
    // reactStrictMode: true,

    // this is for production -  also use ./ for image mapping
    // output:'export',
};

export default nextConfig;
